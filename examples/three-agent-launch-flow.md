# Three-Agent Launch Flow

1. Marlow writes the launch brief.
2. Saul creates the visual system and creates a handoff for motion.
3. Gem renders motion and returns a verification handoff.
4. Publisher ships only after the repo, media, and copy checks pass.

The pattern is not tied to those names. It is a shared project protocol:
memory, roles, handoffs, verification.
